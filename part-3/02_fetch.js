const http = require('http')

const fetch = function (url) {
  return new Promise(() => {
    try {
      // TODO: Promise 및 http.get을 이용해 작성합니다
    }
    catch (err) {
      // TODO: 에러 처리도 필요합니다
    }
  })

};

module.exports = {
  fetch
};
